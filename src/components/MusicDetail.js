import React, {Component} from 'react';

const MusicDetail = ({pros}) =>{
    // if (!music) return null;
    return(
        // <h1>{pros.music}</h1>
        <p>Hello!</p>
    )
};

export default MusicDetail;

